/**
 * PayBound Agent Sandbox — Runtime Entrypoint (Task 2.1)
 *
 * Scaffolds the sandbox environment with no network access by default.
 * Egress rules (task 2.2), workload attestation (task 2.3), payment tool (task 2.4),
 * and agent loop (task 2.5) land in subsequent Phase 2 tasks.
 */

export function main(): void {
  console.log("PayBound Agent Sandbox started (network isolated)");
}

main();
