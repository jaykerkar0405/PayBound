# JSON map of resourceId → max allowed amount (decimal string)
# This secret lives inside the TEE — never visible to the broker
SPEND_CAPS_JSON='{"api-call-gpt4":  "0.50", "api-call-claude": "1.00", "default": "0.25"}'
