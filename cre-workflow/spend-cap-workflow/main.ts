// PLACEHOLDER — implemented in Phase 2 (Issue #98).
//
// This will register a `cre.handlerInTee` confidential workflow that fetches
// SPEND_CAPS_JSON (via secrets.yaml) inside the enclave and evaluates the
// spend-cap decision the broker's checkSpendPolicy() currently stubs.
